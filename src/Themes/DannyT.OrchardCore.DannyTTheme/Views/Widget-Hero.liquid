{% assign backgroundType = Model.ContentItem.Content.Hero.BackgroundType.Text %}
{% assign socialMenu = Content.ContentItemId[Model.ContentItem.Content.Hero.SocialMenu.ContentItemIds] %}
{% assign socialMenuAlias = "alias:" | append: socialMenu.Content.AliasPart.Alias %}

<section class="section section--hero section--center">
    <div class="section__content">
        <div class="hero">
            <div class="hero__body">
                <h1 class="hero__title">{{ Model.ContentItem.Content.Hero.DisplayText.Text | raw }}</h1>

                {% shape "menu", alias: socialMenuAlias %}

                {% if Model.ContentItem.Content.Hero.CTALabel.Text != nil %}
                    <a href="{{ Model.ContentItem.Content.Hero.CTAUrl.Text | href }}" class="btn__btn btn btn--primary">{{ Model.ContentItem.Content.Hero.CTALabel.Text | raw }}</a>
                {% endif %}
            </div>
            
            {% if backgroundType == "embed" %}
                <div class="hero__media">
                    <div class="embed">
						<div class="embed__source">
							<iframe src="{{ Model.ContentItem.Content.Hero.BackgroundEmbed.Text }}" frameborder="0" webkitAllowFullScreen mozallowfullscreen allowFullScreen></iframe>
						</div>
                    </div>
                </div>
            {% else %}
                {{ Model.Content | shape_render }}
            {% endif %}
        </div>
    </div>
</section>